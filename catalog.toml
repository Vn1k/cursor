# Index of every plugin this source ships. The host reads it with
# `git show HEAD:catalog.toml`, so a change only takes effect once committed and
# pushed. cursor/plugin.toml stays authoritative and is re-read on enable; keep
# `version` here in step with it.

[[plugin]]
id = "vn1k/cursor"
name = "Cursor"
version = "1.0.0"
updated_at = 1789995111
added_at = 1789971395
author = "Vn1k"
license = "MIT"
icon = "pointer"
description = "Pick, import and build cursor themes, then apply them everywhere at once."
deprecated = false
plugin_api = 8
tags = ["panel", "shortcut", "theming", "utility", "niri", "hyprland", "sway", "mangowc"]
